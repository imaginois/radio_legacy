(function () {
  'use strict';
  define(['js/core/router'], function (router) {
    describe("Router.js testing URL navigation", function () {

      it("should be able to be instanced", function () {
      	// var man = new manager();
      	// console.log(man);
        expect(true).toBeTruthy();
      });

    });
  });
}());
